// leave the dense datanode llir intact
// build up another array of type info, some nodes empty
// others have type info, when the compiler needs type info, it can use idx

pub struct TypeChecker {
  // todo
}
